# Dependências para Verificador de Integridade - Versão Excel
# Para instalar: pip install -r requirements_windows.txt

# Core dependencies
hashlib2>=1.3.1

# Excel support (opcionais - serão instaladas automaticamente se necessário)
pandas>=1.3.0
openpyxl>=3.0.0

# Build dependencies (apenas para criar executável)
pyinstaller>=6.0.0
auto-py-to-exe>=2.20.0

# Opcional: Para análise de outros formatos
PyPDF2>=3.0.0
python-docx>=0.8.11
lxml>=4.6.0
